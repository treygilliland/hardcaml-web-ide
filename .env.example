# API Configuration
# Rate limiting: maximum number of compile requests per minute per IP
RATE_LIMIT_PER_MINUTE=10

# Server Port (optional, defaults to 8000)
PORT=8000

# CORS Configuration
# Comma-separated list of allowed origins for API requests
# For production with Railway + GitHub Pages:
#   CORS_ORIGINS=https://hardcaml.tg3.dev
# For local development:
#   CORS_ORIGINS=http://localhost:4321,http://localhost:8080
# IMPORTANT: Never use wildcard (*) in production with credentials enabled
CORS_ORIGINS=http://localhost:4321,http://localhost:5173,http://localhost:8080

# Frontend Analytics (PostHog)
# Set to 'false' to disable analytics, 'true' to enable (even on localhost)
VITE_POSTHOG_ENABLED=false

# Your PostHog project API key
VITE_PUBLIC_POSTHOG_KEY=

# PostHog host URL (defaults to https://us.i.posthog.com)
VITE_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# GitHub Container Registry Configuration
# Optional: Override to build/push your own images
# Defaults to ghcr.io/treygilliland/ (publicly available images)
GITHUB_USERNAME=treygilliland

# Docker Image Configuration (optional overrides)
# Base image name (defaults to ghcr.io/treygilliland/hardcaml-base:latest)
BASE_IMAGE=ghcr.io/treygilliland/hardcaml-base:latest

# Production image name (defaults to ghcr.io/treygilliland/hardcaml-web-ide:latest)
PROD_IMAGE=ghcr.io/treygilliland/hardcaml-web-ide:latest

# Docs site image name (defaults to ghcr.io/treygilliland/hardcaml-docs:latest)
DOCS_IMAGE=ghcr.io/treygilliland/hardcaml-docs:latest

# Railway Deployment (optional)
# GitHub Personal Access Token with 'read:packages' permission
# Only needed if using private Docker images
DOCKER_AUTH_TOKEN=

# Frontend API Configuration
# Base URL for the API backend (used by IDE to make compile requests)
#
# For GitHub Pages deployment:
#   PUBLIC_API_BASE_URL=https://api.hardcaml.tg3.dev
# For local development (defaults):
#   PUBLIC_API_BASE_URL=http://localhost:8000
# For docker-compose production testing:
#   PUBLIC_API_BASE_URL=http://localhost:8000
#
# This is passed to the frontend build process and baked into the static files
PUBLIC_API_BASE_URL=
