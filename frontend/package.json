{
  "name": "hardcaml-frontend",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ide dev",
    "dev:docs": "pnpm --filter docs dev",
    "build": "pnpm --filter ide build",
    "build:docs": "pnpm --filter docs build",
    "lint": "pnpm -r lint",
    "preview": "pnpm --filter ide preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
